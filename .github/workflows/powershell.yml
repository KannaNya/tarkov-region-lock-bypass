name: PowerShell syntax

on:
  push:
  pull_request:

jobs:
  syntax:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check PowerShell scripts
        shell: pwsh
        run: ./tests/syntax-check.ps1

